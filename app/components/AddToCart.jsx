import React from "react";

const AddToCart = ({ onClick }) => {
  return (
    <div className="bg-white text-black m-2 cursor-pointer">
      <button className="p-2">Add To Cart</button>
    </div>
  );
};

export default AddToCart;
